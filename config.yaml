# Deep Structural Analysis — 配置
# 本文件为文档性配置：执行以 SKILL.md 为准。default 与 enabled 开关为维护参考，
# 运行时行为由用户请求与 SKILL.md 触发条件决定（行为实验验证：配置项从未被执行路径读取）。

depth:
  # 默认分析深度（用户未指定时）
  default: Standard

language:
  # 输出语言：zh = 全中文，en = 全英文；模型按 config 读取，无法读取时按用户提问语言判断
  default: zh
